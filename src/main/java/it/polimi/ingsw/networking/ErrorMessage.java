package it.polimi.ingsw.networking;

/**
 * a message sent from the server to the client when the username is refused, can be because it's already taken
 * or because it's an empty string
 */
public class ErrorMessage implements Message{
}
